#include <stdio.h>

/**
 * main - Print the alphabet
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alp[26] = "abcdefghijklmnopqrstuvwxyz";
		int i;
	char alp[26] = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
		int x;

	for (i - 0; i < 26; i++)
		{
                	putchar(alp[i]);
		}
	for (x - o; x < 26; x++)
		{
			putchar(alp[i]);
		}
        putchar('\n');
        return (0);
}
